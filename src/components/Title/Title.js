import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Beer Clicky Game!</h1>
        <p>Click on a beer to earn points, but don't click on any more than once!</p>
    </div>
);
export default Title;
